
interface particleModelType {
    count: number
    particleSize: number
    spaceSize: number
    texturePath?: string
    disableRotate?: boolean
    color?: string
}

export type {
    particleModelType
}