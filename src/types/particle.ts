
interface particleModelType {
    count: number
    particleSize: number
    spaceSize: number
    texturePath?: string
    disableRotate?: boolean
    color?: string
    speed?: number
}

export type {
    particleModelType
}